<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>
        <%=data.name%>
    </title>
</head>

<body>
    <div class="d-flex flex-column">
        <h1 class="d-flex justify-content-center mb-5">
            <%=data.name%>
        </h1>
        <div class="d-flex justify-content-center flex-wrap">
            <div class="card p-3" style="width: 30rem;">
                <img src=<%=data.img%> class="card-img-top hover-opacity" alt="product">
                <div class="card-body">
                    <p class="h5">Price: $<%=data.price%>
                    </p>
                    <p class="card-text">Quantity: <%=data.qty%>
                    </p>
                    <p class="card-text text-muted">
                        <%=data.description%>
                    </p>
                </div>
            </div>
            </a>
        </div>
        <div class="d-flex justify-content-center mt-2 mb-2">
            <form class="p-1" action='/api/buy/<%=data._id%>?_method=PUT' method="POST">
                <input type="submit" class="btn btn-success" value="Buy" />
            </form>
            <a class="p-1" href='/edit/<%=data._id%>'><button type="button" class="btn btn-warning">Edit</button></a>
            <form class="p-1" action='/api/delete/<%=data._id%>?_method=DELETE' method="POST">
                <input type="submit" class="btn btn-danger" value="Delete" />
            </form>
            <a class="p-1" href='/store'><button type="button" class="btn btn-primary">Main
                    Menu</button></a>
        </div>
    </div>
</body>

</html>