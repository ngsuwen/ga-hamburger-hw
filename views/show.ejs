<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title><%=data.name%></title>
</head>
<body>
    <div class="d-flex flex-column">
    <h1 class="d-flex justify-content-center mb-5"><%=data.name%></h1>
    <div class="d-flex justify-content-center flex-wrap">
        <div class="card p-3" style="width: 18rem;">
            <img src=<%=data.img%> class="card-img-top hover-opacity" alt="product">
            <div class="card-body">
                <h5 class="card-title"><%=data.name%></h5>
                <p class="card-text">Price: $<%=data.price%></p>
                <p class="card-text">Quantity: <%=data.qty%></p>
            </div>
        </div>
        </a>
    </div>
    <form class="d-flex justify-content-center mt-5" action='/api/buy/<%=data._id%>?_method=PUT' method="POST">
        <input type="submit" class="btn btn-primary" value="Buy Product"/>
    </form>
    <a class="d-flex justify-content-center mt-1" href='/edit/<%=data._id%>'><button type="button" class="btn btn-primary">Edit Product</button></a>
    <form class="d-flex justify-content-center mt-1" action='/api/delete/<%=data._id%>?_method=DELETE' method="POST">
        <input type="submit" class="btn btn-danger" value="Delete Product"/>
    </form>
    <a class="d-flex justify-content-center mt-1" href='/store'><button type="button" class="btn btn-primary">Back to Main</button></a>
    </div>  
</body>
</html>